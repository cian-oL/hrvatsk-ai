[project]
name = "lesson-planner"
version = "0.1.0"
description = "Lesson Planning Agent for each session"
readme = "README.md"
authors = [
    { name = "cian-oL" }
]
requires-python = ">=3.11"
dependencies = [
    "langgraph>=1.0.4",
    "loguru>=0.7.3",
    "pydantic-settings>=2.12.0",
]

[build-system]
requires = ["uv_build>=0.9.15,<0.10.0"]
build-backend = "uv_build"
